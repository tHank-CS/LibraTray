# LibraTray 项目基线与总体要求

> 本文件是 LibraTray 仓库的项目级工作约定和长期基线。
>
> 任何维护者或自动化开发代理在分析、修改、测试、发布本项目之前，必须先完整阅读本文件，
> 再按任务需要阅读相关 ADR、协议文档和测试指南。若实现、文档或新需求与本文件冲突，不得
> 静默绕过；应先说明冲突、证据和影响，再经维护者确认后同步更新本文件。

## 1. 当前基线

- 项目名称：LibraTray
- GitHub 仓库：<https://github.com/hank-wang-cs/LibraTray>
- 当前稳定版本：[`v1.0.0`](https://github.com/hank-wang-cs/LibraTray/releases/tag/v1.0.0)
- 基线日期：2026-08-02
- 目标平台：Windows 10 21H2（内部版本 19044）或更高版本、Windows 11、x64
- 技术栈：C#、.NET 10 LTS、WPF，以及少量隔离的 Win32 互操作
- 开源许可证：Apache-2.0
- 默认交流语言：面向维护者的讨论优先使用简体中文；代码标识符、技术注释、提交信息和
  GitHub 协作文本遵循仓库现有英文惯例

LibraTray 是一个面向 Yeelight Libra Pro 的托盘优先、纯局域网 Windows 控制器，重点是
快速控制、双通道状态同步、实体控制器协同和 Windows 生命周期联动。它不是通用 Yeelight
客户端，也不是 Yeelight 或小米的官方项目。

## 2. 本次项目建设纪要

本项目从空仓库开始，按研究、协议探测、实机验证、正式实现、Windows 集成和发布验证的
顺序完成。以下结论构成后续工作的历史背景。

### 2.1 研究与架构

- 调查了产品身份、公开 Yeelight LAN 协议、同类开源实现、许可证和 Windows 桌面技术栈。
- 比较候选方案后采用 .NET 10 LTS + WPF；协议、领域状态和 UI 保持分层，协议核心不得依赖
  WPF。
- 建立了产品身份、竞品、协议来源、架构、隐私安全、测试、发布和 ADR 文档。
- 选择 Apache-2.0，并记录所有第三方依赖及其许可证。

### 2.2 协议探测与实机证据

- 在真实 YLTD003、内部型号 `lamp15`、固件 38 上完成 UDP 发现、TCP 查询和受保护写入测试。
- 多网卡 Windows 主机可能需要把发现套接字显式绑定到物理局域网 IPv4；不能仅因首次发现
  超时就判断设备离线。
- 验证主灯与氛围灯可独立读取和控制，状态组合包括双开、仅主灯和仅氛围灯。
- 验证主灯亮度、3000–6500 K 色温，以及氛围灯电源、亮度和整灯 RGB。
- 实体控制器通知总体及时，但固件 38 独立关闭氛围灯时可能错误上报 `bg_power=on`；后续
  `get_prop` 查询才是该场景的可信校正依据。
- `set_power` 的结果会受到厂商应用中“氛围灯跟随主灯开关”设置影响，不能把该厂商策略误判
  为网络协议或应用错误。
- 左右分区 RGB 的即时效果和左右顺序曾在实机上成功验证；但其后出现的冷启动异常尚未完成
  因果隔离，因此该能力只保留为研究证据，正式产品继续禁用 `set_segment_rgb`。
- 固件 38 在冷断电重启后可能出现氛围灯“命令返回成功但无亮度、实体控制器和厂商应用普通
  调节也无效”的渲染器沉默问题。一次受控的 `bg_set_scene` 情景刷新能恢复当前运行周期，
  随后需要恢复此前确认的氛围灯外观，并确认主灯状态未被改变。

### 2.3 正式控制与可靠性

- 实现可信发现、精确设备绑定、双通道独立状态、主动通知合并和复读校正。
- 写入必须有结果确认和写后复读；超时、设备忙、协议失败或写后状态不符时，采用有界延迟
  重试，不得直接丢弃用户本次操作。
- 快速连续操作必须被排队、合并或替换，并从最新确认状态计算后续目标；不得产生无界请求
  风暴。
- 同一 TCP 会话中的请求至少间隔 500 ms，并受每分钟 55 条的滚动上限约束，低于公开协议
  的每连接限制。
- 可重试操作最多进行两次延迟重试；重试期间设备保持“在线但忙碌/同步中”的语义，不能仅因
  一次同步失败显示为离线。
- 冷启动氛围灯恢复每个连接周期最多执行一次，不能把普通 TCP 重连都当作设备冷启动。

### 2.4 Windows 应用与交互

- 完成托盘优先的快速控制面板、实时托盘菜单、设备信息、设置、本地预设和可选 OSD。
- 普通手动启动时显示快速面板，避免用户误以为程序未运行；Windows 开机自启时保持安静，
  仅进入托盘。
- 托盘左键立即显示或隐藏快速面板；中键切换主灯；可选滚轮调节主灯亮度。双击切换主灯已
  取消，避免与单击面板行为发生时序冲突。
- 滑块支持点击轨道和拖动松开提交；高频中间值需要合并，最终值必须发送。
- 托盘菜单在打开期间也必须跟随忙碌、连接和电源状态刷新，操作结束后不得遗留灰色按钮。
- 快捷键支持真实按键捕获、自定义、冲突提示和无重复注册；某个快捷键冲突不得导致其他
  快捷键或整个程序失效。
- 本地预设选择器无预设时显示本地化空状态提示，有预设时只显示预设名称，不暴露对象序列化
  字符串。
- 深色主题下的下拉框、复选框、标签页、滚动条和托盘菜单均使用一致的深色模板；不能泄漏
  系统默认白色表面。
- 普通用户界面不得展示 `lamp15`、局域网端点、协议能力列表或实现规范说明；这些内容只可
  出现在高级设备信息、诊断日志、协议文档和开发工具中。

### 2.5 Windows 自动化、打包与发布

- Windows 锁屏、显示器电源、关机状态保存、下次开机恢复和当前用户开机自启均为独立可选
  功能，默认行为必须保守。
- 对不使用睡眠的场景，采用短期、精确设备的一次性关机恢复票据；只有程序随后由 Windows
  自启并满足恢复条件时才恢复，不能无限期保存或覆盖用户之后的手动操作。
- 完成自有品牌图标，并用于可执行文件、应用窗口、托盘、开始菜单快捷方式和安装器。
- MSI 为当前用户安装，提供欢迎、许可证、安装目录、确认、进度、完成及维护/卸载向导；
  ZIP 为自包含便携版。
- `v1.0.0` 已通过自动化检查、真实设备操作、UI、托盘、快捷键、Windows 自动化，以及 MSI
  安装和卸载验证后发布。发布工作流从版本标签构建 ZIP、MSI 和 `SHA256SUMS.txt`，先创建
  草稿 Release，必须经维护者审阅后才能公开。

## 3. 产品身份与支持边界

产品身份字段必须始终分离，不能压扁为一个不可恢复的名称：

```text
FriendlyProductName  项目映射的友好名称，例如 Yeelight Libra Pro
HardwareModel        项目映射的硬件型号，例如 YLTD003
InternalModel        设备协议原始型号，例如 lamp15
ReportedName         设备上报名称
UserAlias            用户在本地设置的别名
```

身份规则：

1. 只有经过清理、忽略大小写后的精确 `lamp15` 匹配才能映射为 Yeelight Libra Pro / YLTD003。
2. 禁止通过名称中含有 `Libra`、`Pro`、`Screen Light Bar` 等子串推断身份。
3. Libra、Libra 2、Pro2、Pura、Pura Pro、YLTD001 和未知型号不得自动映射为本设备。
4. 普通界面显示顺序为 `UserAlias`、`FriendlyProductName`、安全的 `ReportedName`，最后才是
   “未知 Yeelight 设备”。
5. 用户别名不能覆盖或改写原始产品身份。
6. 未经单独研究、实机证据和测试，不得把支持范围扩展到其他设备或固件。

## 4. 不可破坏的设计原则

### 4.1 本地优先与隐私

- 控制仅通过可信局域网进行，不依赖米家或 Yeelight 云端接口。
- 不需要也不得收集厂商账号、密码、云 Token 或设备 Token。
- 不开放公网服务，不运行本地 Web 服务，不上传遥测或日志。
- Yeelight LAN 协议是明文协议，只能在可信网络中使用；不得宣传其具备传输加密或认证。
- 设备返回的网络数据始终按不可信输入处理，限制帧和日志大小，绝不执行返回内容。
- 默认诊断必须脱敏 IP、MAC、设备 ID、主机名、用户名、绝对路径、无线网络名称及同类标识。

### 4.2 分层和依赖方向

依赖只能由外向内：

1. 传输层：UDP 发现、TCP 生命周期、CRLF 分帧、超时、取消和大小限制。
2. 通用协议层：请求、结果、错误、`props`、ID 关联、能力和速率限制。
3. 产品身份层：型号规范化、友好名称、硬件型号、设备名和用户别名。
4. 设备适配层：通用能力和仅包含已验证行为的 Libra Pro 适配器。
5. 状态层：确认状态、待处理状态、来源、可信度、序号和时间。
6. 应用服务层：发现、设置、预设、日志、本地化、托盘、快捷键和 Windows 生命周期。
7. 展示层：快速面板、设置、设备详情、OSD 和诊断界面。

协议和领域项目不得引用 WPF；产品身份不得依赖网络传输；UI 不得直接操作原始 JSON 或套接字；
网络操作不得阻塞 UI 线程。

### 4.3 状态真实性

- 主灯和氛围灯始终是两个独立通道；一个通道关闭不代表设备离线。
- UI 的最终状态以设备确认结果为准，乐观状态必须能回滚或标记不确定。
- 设备通知、查询结果、命令结果和实体控制器变化可能交错，必须按请求 ID、顺序和时间合并。
- 对已知不可靠通知必须进行低频查询校正；不能用高频轮询掩盖状态模型问题。
- 连接中断后应重连并重新查询真实状态；待处理操作不得无界保留或静默取消。
- JSON 拆包、粘包、乱序响应、未知字段、错误 JSON、超大帧和断线都必须有明确、有界的处理。

### 4.4 设备专用行为准入

任何设备专用功能在进入正式适配器或 UI 前，至少需要：

1. 明确来源和证据等级；
2. 记录硬件型号、内部型号和固件版本；
3. 记录脱敏后的请求、响应、通知和写后查询；
4. 验证主灯与氛围灯的副作用；
5. 在重连和必要的冷断电测试后仍安全；
6. 有自动化测试或明确记录无法自动化的原因；
7. 更新协议文档、测试指南和变更日志。

一次成功的视觉效果、第三方项目实现或设备返回 `ok` 都不足以证明功能可安全上线。

## 5. 用户体验基线

- 产品应像紧凑、克制的 Windows 实用工具，不使用大面积渐变、电竞控制中心布局、厚重阴影、
  无意义 3D 装饰或低信息密度大卡片。
- 支持浅色、深色和跟随系统主题，以及简体中文、英文和跟随系统语言。
- 支持常用 Windows 缩放比例、键盘导航、明确焦点、加载、空状态、禁用、成功和错误状态。
- 状态不能只靠颜色表达；网络失败需要给出可理解且可恢复的反馈。
- UI 控件在设备忙碌期间可临时禁用，但操作完成、失败或重试结束后必须自动刷新。
- 普通用户文案说明“能做什么”和“发生了什么”，不展示内部协议、实现策略或安全口号。
- 首次运行和普通启动必须容易发现应用；关闭面板只是隐藏到托盘，只有明确选择“退出”才结束
  进程。

## 6. 配置与 Windows 集成基线

- 配置存放在当前 Windows 用户的应用数据目录，采用版本化 schema、验证、原子替换和损坏
  恢复。
- 配置可以包含设备选择、手动 IP、用户别名、步进、快捷键、预设、主题、语言、开机自启和
  Windows 自动化开关，但不得包含账号凭据或无关个人信息。
- 导入配置必须限制文件大小、验证 schema、先预览后明确保存；导出前提示可能包含本地偏好。
- 单实例互斥必须防止多个进程争用设备、托盘和快捷键。
- 某个 Win32 能力失败时应局部降级并给出反馈，不能导致整个应用退出。
- Windows 自动化不得覆盖用户刚完成的手动操作，必须防抖、记录触发原因并等待网络恢复。

## 7. 测试与质量门禁

所有代码变更都应先运行最窄的相关测试，再按风险扩大验证。正式合并前的标准自动化命令为：

```powershell
powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\restore.ps1 -Locked
powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File .\scripts\verify.ps1 -SkipRestore
```

正式发布还必须执行仓库提供的发布构建、打包和 SHA-256 生成流程，并检查产物内容。具体步骤以
[`RELEASING.md`](RELEASING.md) 为准。

质量要求：

- 不得声称未实际运行的构建、测试、安装或硬件行为已经通过。
- 协议、状态、配置、重试和已修复缺陷应尽可能增加回归测试。
- 模拟设备测试只能证明软件逻辑，不能替代真实 YLTD003 固件证据。
- UI 变更除编译外还需验证真实交互路径、主题、缩放和忙碌/失败后的恢复状态。
- 文件处理、日志和配置测试不得破坏用户真实数据。
- 不得为得到绿色结果而删除、跳过或弱化现有测试。

## 8. 文档与证据等级

使用以下固定术语，避免把推测写成事实：

- **官方文档**：Yeelight 公开 LAN 规范明确说明。
- **开源参考**：在已记录许可证的公开实现中观察到。
- **设备验证**：在记录了硬件、固件和完整结果的目标设备上复现。
- **未验证**：尚未在目标设备上复现。
- **已知固件问题**：可重复且已限定固件范围。
- **需要回退**：应用必须使用查询、重连或其他更安全路径。

涉及协议、产品身份或硬件行为的修改，应同步检查并更新：

- [`docs/protocol/lamp15.md`](docs/protocol/lamp15.md)
- [`docs/protocol/state-synchronization.md`](docs/protocol/state-synchronization.md)
- [`docs/testing-guide.md`](docs/testing-guide.md)
- [`CHANGELOG.md`](CHANGELOG.md)

涉及架构、技术栈或许可证的变化必须新增或更新 ADR；不得只修改代码。

## 9. Git、协作与发布要求

- 修改前检查工作区，保留用户现有改动；禁止用破坏性 Git 命令清理工作区。
- 进行最小且完整的变更，不做无关重构、全仓格式化或顺手升级依赖。
- 提交应小而清晰，提交信息、分支名和 PR 标题优先使用英文。
- 未经明确授权，不得提交、推送、合并、创建 PR 或发布 Release。
- 发布遵循语义化版本。标签必须指向预期的 `main` 提交，Release 必须先生成草稿并审计：
  自动化结果、版本号、发行说明、ZIP/MSI 内容、SHA-256、已知限制和未签名提示。
- 当前安装包未进行代码签名，必须如实提示 SmartScreen/未知发布者风险，不得宣称可信签名。
- 依赖必须锁定或记录版本；新增生产依赖前检查维护状态、兼容性、许可证、体积和替换成本。

## 10. 当前明确限制与后续决策边界

以下事项不得被后续对话误当作普通待办直接启用：

1. **左右分区 RGB**：即时效果已验证，但安全性未证明，正式产品保持禁用。只有在独立测试序列
   中排除冷启动异常因果关系，并完成冷断电、重连和副作用验证后才能重新评估。
2. **固件 38 氛围灯冷启动沉默**：当前恢复属于有界运行时缓解，不是固件修复；不得频繁或
   无条件发送情景刷新。
3. **固件通知可信度**：独立氛围灯关灯通知可能错误，不能移除查询校正。
4. **代码签名**：尚未完成。若未来引入签名，密钥必须通过安全的外部机密管理提供，绝不能
   提交到仓库或日志。
5. **其他设备支持**：v1.0.0 仅把精确 `lamp15` / YLTD003 作为一等支持对象；扩展型号需要
   单独身份映射、适配器、实机证据和兼容性说明。
6. **干净虚拟机验证**：v1.0.0 已完成人工安装卸载测试，但未宣称完成独立干净虚拟机验证。

## 11. 新任务开始与结束检查表

开始任何新任务时：

1. 阅读本文件。
2. 检查 `git status`，阅读与任务相关的 README、架构、ADR、协议和测试文档。
3. 识别请求是否会改变产品边界、设备命令、状态模型、Windows 自动化或发布行为。
4. 如果涉及实机行为，先说明证据等级和安全测试顺序；未知命令默认不发送。
5. 选择不破坏现有可靠性和用户配置的最小完整实现。

完成任务时：

1. 检查完整 diff，排除无关修改和敏感信息。
2. 运行与风险相称的格式、构建、测试和真实交互验证。
3. 更新相应文档、测试和 `CHANGELOG.md`。
4. 清楚报告已验证内容、未运行检查、剩余限制和使用方式。
5. 如果任务改变了本文件中的事实或决策，同步更新本文件，避免基线与实现漂移。
